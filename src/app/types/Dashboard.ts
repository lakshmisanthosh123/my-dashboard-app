// src/types/Dashboard.ts
export interface Dashboard {
    id: string; // Unique identifier for each dashboard
    name: string; // Name of the dashboard
    dateCreated: string; // Date when the dashboard was created
    layout: any[]; // Layout information for the dashboard
  }
  